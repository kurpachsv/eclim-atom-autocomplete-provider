'use babel';

import EclimAtomAutocompleteProviderView from '../lib/eclim-atom-autocomplete-provider-view';

describe('EclimAtomAutocompleteProviderView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
