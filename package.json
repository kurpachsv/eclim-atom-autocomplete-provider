{
  "name": "eclim-atom-autocomplete-provider",
  "main": "./lib/main.js",
  "version": "0.0.1",
  "description": "Suggestions provider for eclim-atom package",
  "keywords": [
  ],
  "activationCommands": {
    "atom-workspace": "eclim-atom-autocomplete-provider:toggle"
  },
  "repository": "https://github.com/kurpachsv/eclim-atom-autocomplete-provider.git",
  "license": "MIT",
  "engines": {
    "atom": ">=1.0.0 <2.0.0"
  },
  "providedServices": {
    "autocomplete.provider": {
      "versions": {
        "2.0.0": "getProvider"
      }
    }
  },
  "dependencies": {
    "child_process": "^1.0.2"
  }
}
